<%= form_with(model: house) do |form| %>
  <% if house.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(house.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
        <% house.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :housename, style: "display: block" %>
    <%= form.text_field :housename %>
  </div>

  <div>
    <%= form.label :houselocation, style: "display: block" %>
    <%= form.text_field :houselocation %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
